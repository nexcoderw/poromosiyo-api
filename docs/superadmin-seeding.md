# SUPERADMIN Seeding

## Purpose

The initial Poromosiyo SUPERADMIN is seeded from protected API environment
configuration.

Real SUPERADMIN credentials must never be committed to Git.

## Environment

The local API `.env` contains:

```text
SUPERADMIN_FULL_NAME=
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=
```
